
■■■ 実行ファイル作成手順 ■■■

１．Eclipseで「build.xml」を右クリック >「実行」>「Antビルド」を実行する。

２．buildフォルダに「FileDownload.jar」が作成される。

３．実行フォルダに以下のファイルをコピー。
      ・FileDownload.jar
      ・FileDownload.bat
      ・FileDownload.vbs
      ・confフォルダ
      ・libフォルダ

４．「FileDownload.vbs」をダブルクリックするとシステムが起動する。


